.env
__pycache__/
*.pyc
*.pyo
.DS_Store
venv/
.venv/
*.egg-info/
dist/
build/
engagement_log.csv
tweet_history.json
replied_tweets.json
followed_accounts.json
daily_state.json
performance_log.json
learnings.json
bot.log
bot.log.*

# Evolution agent runtime artifacts (regenerated)
directives.md
pruned_accounts.json
reinforced_accounts.json
evolution_log.json

# Autonomous operator cross-cycle state (used by remote cron)
autonomous_state.json

# Runtime state — generated by the bot, not source-controlled
quote_daily_state.json
quoted_tweets.json

# Strategy agent runtime artifacts (regenerated each cycle)
dynamic_queries.json
dynamic_accounts.json
strategy_log.json

# Manual kill-switch for /stop (must NOT be tracked or it would
# permanently disable the watchdog after a fresh clone).
.bot_disabled

# Ephemeral local state — never useful in git.
bot.err
.llm_rate_state.json
test_gemini_integration.py
bot.lock

# Agentic-loop runtime artifacts (auto_improve daily run + self-heal trigger).
auto_improve.log
.auto_improve_running
.last_self_heal
.claude/scheduled_tasks.lock
claude_lockout.json

# Operator-private session notes — loaded by Claude Code, never committed
# (repo is on GitHub; private notes live here instead of CLAUDE.md).
CLAUDE.local.md

# secrets — never track (tokens live here)
.env

# runtime agent artifacts
watchdog.log
suggest.log
suggestions.md

# Editorial publishing state and local reach measurements
editorial_state.json
editorial_review.jsonl
editorial_reach.json
editorial_reach.md

# Scratch outputs of removed flows, untracked in issue #112
run.out
*_output.txt
current_tweet.txt
outgoing_tweet.txt
reply.txt

# Atomic-write temp files left by a killed process
.replied_tweets.*.tmp
action_ledger.json.tmp
