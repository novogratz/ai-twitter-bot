Manage the target accounts list for the engage bot:

1. Read `src/engage_bot.py` and show the current TARGET_ACCOUNTS list, organized by tier
2. Read `followed_accounts.json` and show which ones have been followed already
3. Show the count: X accounts in list, Y already followed
4. Ask the user if they want to add or remove accounts
5. If they want changes, edit `src/engage_bot.py` to update the list
