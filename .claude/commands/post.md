Manually trigger a post cycle right now. Run the bot's post logic once:

1. Read `src/bot.py` to understand the current post logic
2. Run `python -c "from src.bot import run_bot_cycle; run_bot_cycle()"` to trigger one post cycle
3. Report what was posted (news or hot take) and the daily counter status
