Reset daily counters to start fresh. Useful if you want the bot to post more today:

1. Read `daily_state.json` to show current counters
2. Ask the user which counters to reset (news, hotakes, or both)
3. Write the updated `daily_state.json` with reset counters
4. Confirm the reset
