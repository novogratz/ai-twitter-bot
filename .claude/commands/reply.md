Manually trigger a reply cycle right now. Run the reply bot once:

1. Run `python -c "from src.reply_bot import run_reply_cycle; run_reply_cycle()"` to trigger one reply scan
2. Report how many replies were posted and to which tweets
